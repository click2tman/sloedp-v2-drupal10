{% if view_mode == 'teaser' %} 
  <div class="team-block team-teaser-1">
    <div class="team-image">{{ content.field_team_image }}
      <div class="socials">
        
        {% if node.field_team_facebook.value %}
          <a class="gva-social" href="{{ node.field_team_facebook.value }}"><i class="fab fa-facebook"></i></a>
        {% endif %} 
        {% if node.field_team_linkedin.value %}
          <a class="gva-social" href="{{ node.field_team_linkedin.value }}"><i class="fab fa-linkedin"></i></a>
        {% endif %} 
        {% if node.field_team_pinterest.value %}
          <a class="gva-social" href="{{ node.field_team_pinterest.value }}"><i class="fab fa-pinterest"></i></a>
        {% endif %} 
        {% if node.field_team_twitter.value %}
          <a class="gva-social" href="{{ node.field_team_twitter.value }}"><i class="fab fa-twitter"></i></a>
        {% endif %}

      </div>
    </div>
    <div class="team-content">
      <div class="team-job">{{ node.field_team_position.value|e }}</div>
      <div class="team-name"><a href="{{ url }}">{{ node.field_team_name.value|e }}</a></div>   
      <div class="team-skills">
      {% for item in node.field_team_skills %}
        {% set skill = item.value|split('--') %}
          {% if skill['0'] and skill['1'] %}
            <div class="team-skill">
              <div class="progress-label">{{ skill['0'] }}</div>
               <div class="progress">
                 <div class="progress-bar" data-progress-animation="{{ skill['1'] }}%"><span></span></div>
                 <span class="percentage"><span></span>{{ skill['1'] }}%</span>
              </div>
            </div>
          {% endif %}  
      {% endfor %} 
    </div>
    </div>
  </div>  
{% elseif view_mode == 'teaser_2' %} 
  <div class="team-block team-teaser-2">
    <div class="team-block-inner">
      <div class="team-image">{{ content.field_team_image }}
        <div class="socials">
          {% if node.field_team_facebook.value %}
            <a class="gva-social" href="{{ node.field_team_facebook.value }}"><i class="fab fa-facebook"></i></a>
          {% endif %} 
          {% if node.field_team_linkedin.value %}
            <a class="gva-social" href="{{ node.field_team_linkedin.value }}"><i class="fab fa-linkedin"></i></a>
          {% endif %} 
          {% if node.field_team_pinterest.value %}
            <a class="gva-social" href="{{ node.field_team_pinterest.value }}"><i class="fab fa-pinterest"></i></a>
          {% endif %} 
          {% if node.field_team_twitter.value %}
            <a class="gva-social" href="{{ node.field_team_twitter.value }}"><i class="fab fa-twitter"></i></a>
          {% endif %} 
        </div>
      </div>
      <div class="team-content">
        <div class="team-job">{{ node.field_team_position.value|e }}</div>
        <div class="team-name"><a href="{{ url }}">{{ node.field_team_name.value|e }}</a></div>   
        <div class="team-body">{{ content.body }}</div>
      </div>
    </div> 
  </div>  
{% elseif view_mode == 'teaser_3' %} 
  <div class="team-block team-teaser-3">
    <div class="team-image">{{ content.field_team_image }}</div>
    <div class="team-content">
      <div class="team-name"><a href="{{ url }}">{{ node.field_team_name.value|e }}</a></div>   
      <div class="team-job">{{ node.field_team_position.value|e }}</div>   
    </div>
  </div>  
{% elseif view_mode == 'teaser_4' %} 
  <div class="team-block team-teaser-4">
    <div class="team-image">{{ content.field_team_image }}</div>
    <div class="team-content">
      <div class="team-name"><a href="{{ url }}">{{ node.field_team_name.value|e }}</a></div>   
      <div class="team-job">{{ node.field_team_position.value|e }}</div>
      <div class="socials">
        {% if node.field_team_facebook.value %}
          <a class="gva-social" href="{{ node.field_team_facebook.value }}"><i class="fab fa-facebook"></i></a>
        {% endif %} 
        {% if node.field_team_linkedin.value %}
          <a class="gva-social" href="{{ node.field_team_linkedin.value }}"><i class="fab fa-linkedin"></i></a>
        {% endif %} 
        {% if node.field_team_pinterest.value %}
          <a class="gva-social" href="{{ node.field_team_pinterest.value }}"><i class="fab fa-pinterest"></i></a>
        {% endif %} 
        {% if node.field_team_twitter.value %}
          <a class="gva-social" href="{{ node.field_team_twitter.value }}"><i class="fab fa-twitter"></i></a>
        {% endif %}
      </div>
    </div>
  </div>  
{% else %}

{%
  set classes = [
    'node',
    'node-detail',
    'node--type-' ~ node.bundle|clean_class,
    node.isPromoted() ? 'node--promoted',
    node.isSticky() ? 'node--sticky',
    not node.isPublished() ? 'node--unpublished',
    view_mode ? 'node--view-mode-' ~ view_mode|clean_class,
    'clearfix',
  ]
%}

<article{{ attributes.addClass(classes) }}>
  <div class="team-single-page">
    <div class="row">
      <div class="col-md-3 col-sm-3 col-xs-12">
        <div class="team-image">{{ content.field_team_image }}</div>
        <div class="team-election-details margin-top-10">   
            <div class="content-inner">{{ content.field_candidate_surname.value|e }}, {{ content.field_candidate_first_name.value|e }}</div>
			<div class="content-inner">{{ node.field_team_position.value|e }}</div>
            <div class="content-inner">{{ content.field_election_political_party.value|e }}</div>
            <div class="content-inner">{{ content.field_candidate_gender.value|e }}</div>
            <div class="content-inner">{{ content.field_election_year.value|e }}</div>
            <div class="content-inner">{{ content.field_election_type.value|e }}</div>
            {% if node.field_city_council.value %}
              <div class="content-inner">{{ content.field_city_council.value|e }}</div>
            {% endif %}
            {% if node.field_election_result_ward.value %}
              <div class="content-inner">{{ content.field_election_result_ward.value|e }}</div>
            {% endif %}
            {% if node.field_result_constituency.value %}
             <div class="content-inner">{{ content.field_result_constituency.value|e }}</div>
            {% endif %}
            {% if node.field_election_result_district.value %}
             <div class="content-inner">{{ content.field_election_result_district.value|e }}</div>
            {% endif %}
        </div>
        <div class="team-contact margin-top-10">
            <div class="heading">{{'Contact Info'|t}}</div>
            {% if node.field_team_contact.value %}
              <div class="content-inner">{{ content.field_team_contact }}</div>
            {% endif %} 
            {% if node.field_team_email.value %}
              <div class="content-inner">{{ content.field_team_email }}</div>
            {% endif %} 
            {% if node.field_team_website.value %}
              <div class="content-inner">{{ content.field_team_website }}</div>
            {% endif %} 
            <div class="socials">
              {% if node.field_team_facebook.value %}
                <a class="gva-social" href="{{ node.field_team_facebook.value }}"><i class="fab fa-facebook"></i></a>
              {% endif %} 
              {% if node.field_team_linkedin.value %}
                <a class="gva-social" href="{{ node.field_team_linkedin.value }}"><i class="fab fa-linkedin"></i></a>
              {% endif %} 
              {% if node.field_team_pinterest.value %}
                <a class="gva-social" href="{{ node.field_team_pinterest.value }}"><i class="fab fa-pinterest"></i></a>
              {% endif %} 
              {% if node.field_team_twitter.value %}
                <a class="gva-social" href="{{ node.field_team_twitter.value }}"><i class="fab fa-twitter"></i></a>
              {% endif %}
            </div>
          </div>
      </div>
      <div class="col-md-9 col-sm-9 col-xs-12">
        <div class="team-description">{{ content.field_team_description }}</div>
        <div class="team-info">
          <div class="team-education">
            <div class="heading">{{'Education'|t}}</div>
            <div class="content-inner">{{ content.field_team_education }}</div>
          </div>
        </div>

        <div class="team-skills">
          {% for item in node.field_team_skills %}
            {% set skill = item.value|split('--') %}
              {% if skill['0'] and skill['1'] %}
                <div class="team-skill">
                  <div class="progress-label">{{ skill['0'] }}</div>
                   <div class="progress">
                     <div class="progress-bar" data-progress-animation="{{ skill['1'] }}%"><span></span></div>
                     <span class="percentage"><span></span>{{ skill['1'] }}%</span>
                  </div>
                </div>
              {% endif %}  
          {% endfor %} 
        </div>

        <div{{ content_attributes.addClass('node__content', 'clearfix') }}>
          {{ content|without('field_team_name', 'field_election_type', 'field_election_result_district', 'field_result_constituency', 'field_team_website', 'field_candidate_gender', 'field_team_email', 'field_city_council', 'field_election_result_ward', 'field_candidate_surname', 'field_election_political_party', 'field_candidate_other_names', 'field_candidate_first_name', 'field_election_year',  'field_team_contact', 'field_team_image', 'field_team_contact', 'field_team_skills', 'field_team_quote', 'field_team_pinterest', 'field_team_linkedin', 'field_team_facebook', 'field_team_twitter', 'field_team_google', 'field_team_education', 'field_team_position', 'field_team_email', 'field_team_description', 'comment') }}
        </div>
        
        {% if content.field_team_quote|render %}
          <div class="team-quote"> {{content.field_team_quote}} </div>
        {% endif %}  
      </div>
     
    {% if content.comment %}
      <div id="node-single-comment">
        {{ content.comment }}
      </div>
    {% endif %}  

  </div>
</article>

<!-- End Display article for detail page -->
{% endif %}

